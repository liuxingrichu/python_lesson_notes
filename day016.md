【OSI七层协议】
应用层（http smtp dns ftp ssh snmp dhcp POP3 SMTP NFS）
表示层
会话层
传输层（TCP UDP）
网络层（IP icmp 路由）
数据链路层（mac）
物理层

【TCP/IP协议】
应用层，即OSI的应用层、表示层、会话层
传输层
网络层
网络接口层，即OSI的物理层

【通信协议】
（1）TCP/IP 
三次握手、四次断开

（2）UDP